Почти как в С
``` go
package main

import "fmt"

type Vertex struct {
	X int
	Y int
}

func main() {
	v := Vertex{1, 2}
	p := &v
	// Для указателя на структуру запись `(*p).X` можно сократить  
	p.X = 1e9
	fmt.Println(v)
}

```